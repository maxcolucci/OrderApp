spring:
  application:
    name: ordering-system

  datasource:
    url: jdbc:postgresql://localhost:5432/ordering_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080